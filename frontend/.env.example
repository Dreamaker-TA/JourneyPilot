VITE_API_BASE=/api

# VITE_API_BASE: base path for the FastAPI backend the frontend calls.
# Defaults to /api (proxied to http://127.0.0.1:8001 in vite.config.ts).
#
# Do not put LLM provider keys in frontend env files. Live model keys belong on
# the backend only.
